apply plugin: 'kotlin'
apply plugin: 'kotlin-kapt'

dependencies {
    api project(':base_domain')

    implementation libraries.kotlinCoroutinesCore
    implementation libraries.kotlinStdLib

    kapt libraries.daggerCompiler
}
